/**
 * MedLog REST API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Hersteller model module.
 * @module model/Hersteller
 * @version 0.0.1
 */
class Hersteller {
    /**
     * Constructs a new <code>Hersteller</code>.
     * @alias module:model/Hersteller
     * @param aiVersionId {String} Foreing key to 'AiDataVersion' ('GKV WiDo Arzneimittel Index' Data Format Version) which contains the information which Arzneimittel Index 'Datenstand' and 'Dateiversion' the row has
     * @param herstellercode {String} Hersteller
     * @param bedeutung {String} Bedeutung
     */
    constructor(aiVersionId, herstellercode, bedeutung) { 
        
        Hersteller.initialize(this, aiVersionId, herstellercode, bedeutung);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, aiVersionId, herstellercode, bedeutung) { 
        obj['ai_version_id'] = aiVersionId;
        obj['herstellercode'] = herstellercode;
        obj['bedeutung'] = bedeutung;
    }

    /**
     * Constructs a <code>Hersteller</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Hersteller} obj Optional instance to populate.
     * @return {module:model/Hersteller} The populated <code>Hersteller</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Hersteller();

            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('ai_version_id')) {
                obj['ai_version_id'] = ApiClient.convertToType(data['ai_version_id'], 'String');
            }
            if (data.hasOwnProperty('herstellercode')) {
                obj['herstellercode'] = ApiClient.convertToType(data['herstellercode'], 'String');
            }
            if (data.hasOwnProperty('bedeutung')) {
                obj['bedeutung'] = ApiClient.convertToType(data['bedeutung'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Hersteller</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Hersteller</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Hersteller.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ai_version_id'] && !(typeof data['ai_version_id'] === 'string' || data['ai_version_id'] instanceof String)) {
            throw new Error("Expected the field `ai_version_id` to be a primitive type in the JSON string but got " + data['ai_version_id']);
        }
        // ensure the json data is a string
        if (data['herstellercode'] && !(typeof data['herstellercode'] === 'string' || data['herstellercode'] instanceof String)) {
            throw new Error("Expected the field `herstellercode` to be a primitive type in the JSON string but got " + data['herstellercode']);
        }
        // ensure the json data is a string
        if (data['bedeutung'] && !(typeof data['bedeutung'] === 'string' || data['bedeutung'] instanceof String)) {
            throw new Error("Expected the field `bedeutung` to be a primitive type in the JSON string but got " + data['bedeutung']);
        }

        return true;
    }


}

Hersteller.RequiredProperties = ["ai_version_id", "herstellercode", "bedeutung"];

/**
 * @member {Date} created_at
 */
Hersteller.prototype['created_at'] = undefined;

/**
 * Foreing key to 'AiDataVersion' ('GKV WiDo Arzneimittel Index' Data Format Version) which contains the information which Arzneimittel Index 'Datenstand' and 'Dateiversion' the row has
 * @member {String} ai_version_id
 */
Hersteller.prototype['ai_version_id'] = undefined;

/**
 * Hersteller
 * @member {String} herstellercode
 */
Hersteller.prototype['herstellercode'] = undefined;

/**
 * Bedeutung
 * @member {String} bedeutung
 */
Hersteller.prototype['bedeutung'] = undefined;






export default Hersteller;

